<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View" />
        <variable
            name="groupModel"
            type="com.agelousis.monthlyfees.main.ui.payments.models.GroupModel" />
        <variable
            name="presenter"
            type="com.agelousis.monthlyfees.main.ui.payments.presenters.GroupPresenter" />
    </data>

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?attr/selectableItemBackgroundBorderless"
        android:onClick="@{() -> presenter.onGroupSelected(groupModel)}"
        android:paddingStart="@dimen/nav_header_vertical_spacing"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:paddingBottom="@dimen/nav_header_vertical_spacing"
        android:paddingEnd="@dimen/nav_header_vertical_spacing">

    <com.google.android.material.textview.MaterialTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/ubuntu_monospace"
        android:textStyle="bold"
        android:text="@{groupModel.groupName}"
        android:textColor="@{(groupModel.groupHeaderColor != null) ? groupModel.groupHeaderColor : @color/grey}"
        tools:text="Group 1"/>

    </LinearLayout>

</layout>
